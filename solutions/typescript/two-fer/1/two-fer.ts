/**
 * This stub is provided to make it straightforward to get started.
 */

export function twoFer(name: string | undefined): string {
    return name ? `Two for you, two for me.` : `Two for ${name}, two for me.`;
}
